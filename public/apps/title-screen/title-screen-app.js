define(function(require){
  var utils       = require('utils');
  var gamepad     = require('gamepad');

  var app = {
    init: function(){
      
    }

  , router: {
      routes: {
        
      }
    }
  };

  return app;
});